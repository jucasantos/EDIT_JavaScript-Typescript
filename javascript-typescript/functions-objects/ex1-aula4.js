//EXERCÍCIO 1
/*
function sum(x, y) {
    return x + y;
  }
  
  const result = sum(5, 7);
  console.log(result); 

  //OU

  const sum = (a,b) => a + b;

  console.log(sum(1,2));

  //EXERCÍCIO 2

  const calcSquare = (x) => {
    return x * x;
  };
  
  const numSquare = 5;
  const result = calcSquare(numSquare);
  console.log(result);

  //EXERCÍCIO 3

  function getNumber(num) {
    if (num > 0) { //se o num for maior q zero
      return "Positivo";
    } else if (num < 0) { //menor / negativo
      return "Negativo";
    } else {
      return "Zero"; //igual a zero
    }
  }
  
  console.log(getNumber(1)); 

  //EXERCÍCIO 4

  function getNumber(num1) {
    return function getNumber2(num2){
        return num1 + num2;
    }
  }

  const sum = getNumber(5);
  console.log(sum(4)); 
  
  

  //EXERCÍCIO 5

  function firstFunction(){
    console.log("Hello")
    callback();
  }

  function callback(){
    console.log("this is a callback");
  }

  firstFunction(callback); */

  //EXERCÍCIO 6

  const car = {
    type:"Honda",
    year:"2020",
    model:"500"
};











  